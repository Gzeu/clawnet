import { describe, it, expect } from "vitest";

describe("Registry", () => {
  it("should be able to register an agent", () => {
    // Test de bază - va fi extins ulterior
    expect(true).toBe(true);
  });
});